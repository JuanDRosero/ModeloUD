@model IEnumerable<Rol>
@{
    ViewData["Title"] = "Roles Empleado";
}
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        Lista de roles
    </div>
    <div class="card-body">
        <table class="table">
            <thead align="center">
                <tr>
                    <th>@Html.DisplayNameFor(x=>x.Id)</th>
                    <th>@Html.DisplayNameFor(x=>x.Descripcion)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody align="center">
                @if (Model.Count()!=0)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                    <td>@Html.DisplayFor(x=>item.Id)</td>
                    <td>@Html.DisplayFor(x=>item.Descripcion)></td>
                    <td>
                        <a asp-action="Edit" class="btn" asp-route-id="@item.Id">
                            <i class="fa-solid fa-file-pen"></i>
                        </a>
                        <a asp-action="Delete"class="btn"asp-route-id="@item.Id">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>
                <tr>
                    <td>123</td>
                    <td>Tiger Nixon</td>
                    <td>
                        <a asp-action="Edit" class="btn">
                            <i class="fa-solid fa-file-pen"></i>
                        </a>
                        <a class="btn">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>
                <tr>
                    <td>123</td>
                    <td>Tiger Nixon</td>
                    <td>
                        <a asp-action="Edit" class="btn">
                            <i class="fa-solid fa-file-pen"></i>
                        </a>
                        <a class="btn">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>